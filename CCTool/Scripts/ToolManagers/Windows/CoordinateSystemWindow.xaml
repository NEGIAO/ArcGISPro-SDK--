<controls:ProWindow x:Class="CCTool.Scripts.ToolManagers.Windows.CoordinateSystemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mapping="clr-namespace:ArcGIS.Desktop.Mapping.Controls;assembly=ArcGIS.Desktop.Mapping"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="CoordinateSystemWindow" Height="500" Width="500"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="450*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Border BorderBrush="{DynamicResource Esri_BorderBrush}" BorderThickness="2" Margin="2">
                <mapping:CoordinateSystemsControl  x:Name="cod"
                          ConfigureControl="{Binding Path=ControlProperties}"/>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Button x:Name="btn_go" Content="确定" Margin="0,0,21,0" VerticalAlignment="Center" HorizontalAlignment="Right" Width="65" Click="btn_go_Click"/>
        </Grid>

    </Grid>
</controls:ProWindow>
