<controls:ProWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" x:Class="CCTool.Scripts.LayerPross.GetPolygonExtent"
        mc:Ignorable="d"
        Title="面要素计算四至" Height="353" Width="292"
        WindowStartupLocation="CenterOwner" Icon="/CCTool;component/Data/Icons/Atoolbox.png" Loaded="form_Load"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <Button x:Name="btn_go" Style="{StaticResource btn_run}" Margin="0,0,14,10"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_click" />

        <xctk:DecimalUpDown x:Name="txtNum" Margin="135,47,14,0" Height="18" VerticalAlignment="Top" Maximum="10" Minimum="0" DefaultValue="3" DisplayDefaultValueOnEmptyText="True"/>
        <Label Content="XY坐标小数位数：" Margin="20,43,0,0" VerticalAlignment="Top" Height="25" HorizontalAlignment="Left" Width="110"/>
        <CheckBox x:Name="cb_origin" Content="在原图层中标记四至点坐标" HorizontalAlignment="Left" Margin="18,20,0,0" VerticalAlignment="Top" IsChecked="True"/>
        <CheckBox x:Name="cb_point" Content="导出四至点要素图层" HorizontalAlignment="Left" Margin="18,92,0,0" VerticalAlignment="Top" IsChecked="True"/>
        <Label Content="选择要保留的字段：" Margin="20,112,145,0" VerticalAlignment="Top" Height="25"/>
        <ListBox Style="{DynamicResource ListBoxStyle1}" x:Name="listbox_field" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="20,137,14,62"/>
        <Button x:Name="btn_select" Style="{StaticResource btn_selectAll}" Margin="0,115,41,0" Click="btn_select_Click" Height="22" VerticalAlignment="Top" HorizontalAlignment="Right" Width="22" />
        <Button x:Name="btn_unSelect" Style="{StaticResource btn_unSelectAll}" Margin="0,115,14,0" Click="btn_unSelect_Click" HorizontalAlignment="Right" Width="22" Height="22" VerticalAlignment="Top"  />
        <Button x:Name="btn_help" Style="{StaticResource btn_help}" Margin="0,0,99,10" VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_help_Click"/>

    </Grid>
</controls:ProWindow>
