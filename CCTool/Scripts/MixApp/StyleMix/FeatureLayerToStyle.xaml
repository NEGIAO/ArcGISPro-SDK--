<controls:ProWindow x:Class="CCTool.Scripts.MixApp.StyleMix.FeatureLayerToStyle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="图层唯一值符号转样式库" Height="300" Width="400"
                    WindowStartupLocation="CenterScreen"
                    Icon="/CCTool;component/Data/Icons/Atoolbox.png"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>


    <Grid>
        <ComboBox x:Name="combox_fc" Style="{StaticResource ComboBox_icon}"  Margin="20,42,19,0" VerticalAlignment="Top" Height="25" DropDownOpened="combox_fc_DropDown"/>
        <Label Content="选择图层（当前符号系统需为【唯一值】类型）：" HorizontalAlignment="Left" Margin="15,12,0,0" VerticalAlignment="Top" Width="351"/>
        <Button x:Name="btn_go"  Margin="0,0,19,24" Style="{StaticResource btn_run}"   VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_Click"/>
        <Button x:Name="btn_help"  Margin="0,0,101,24" Style="{StaticResource btn_help}"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_help_Click"/>
        <Label Content="选择保存的Stylx（可手动输入，工具将新建Stylx）：" HorizontalAlignment="Left" Margin="15,74,0,0" VerticalAlignment="Top" Width="371"/>
        <CheckBox x:Name="cb_add" Content="追加到选择的Stylx样式库中" HorizontalAlignment="Left" Margin="20,140,0,0" VerticalAlignment="Top"/>
        <ComboBox x:Name="combox_stylx"  Margin="20,104,19,0" VerticalAlignment="Top" Height="25" DropDownOpened="combox_stylx_DropDown" IsEditable="True"/>

    </Grid>
</controls:ProWindow>
