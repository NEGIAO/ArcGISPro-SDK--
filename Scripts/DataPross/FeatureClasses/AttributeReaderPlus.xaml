<controls:ProWindow x:Class="CCTool.Scripts.DataPross.FeatureClasses.AttributeReaderPlus"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:CCTool.Scripts.DataPross.FeatureClasses"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="属性读取强化版" Height="500" Width="470"
                    WindowStartupLocation="CenterScreen"
                    Icon="/CCTool;component/Data/Icons/Atoolbox.png"
    >

    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>

    <Grid>
        <GroupBox Header="参数设置" Margin="15,87,16,0" Height="63" VerticalAlignment="Top"/>

        <ComboBox x:Name="combox_origin_fc" Style="{StaticResource ComboBox_icon}"  Margin="92,12,19,0" VerticalAlignment="Top" Height="25" DropDownOpened="combox_origin_fc_DropDown" DropDownClosed="combox_origin_fc_Closed"/>
        <Label Content="目标图层：" HorizontalAlignment="Left" Margin="15,12,0,0" VerticalAlignment="Top" Width="62"/>
        <ComboBox x:Name="combox_identity_fc" Style="{StaticResource ComboBox_icon}"  Margin="91,48,19,0" VerticalAlignment="Top" DropDownOpened="combox_identity_fc_DropDown" Height="25" DropDownClosed="combox_identity_fc_Closed"/>
        <Label Content="来源图层：" HorizontalAlignment="Left" Margin="15,48,0,0" VerticalAlignment="Top" Width="62"/>
        <Label Content="最小重叠比例：" HorizontalAlignment="Left" Margin="32,110,0,0" VerticalAlignment="Top" Width="89"/>
        <TextBox x:Name="propTXT" HorizontalAlignment="Left" Margin="121,114,0,0" TextWrapping="NoWrap" Text="{Binding ElementName=sliderLine, Path=Value}" VerticalAlignment="Top" Width="43" Height="18"/>
        <Button x:Name="btn_go"  Margin="0,0,19,24" Style="{StaticResource btn_run}"   VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_Click"/>

        <Button x:Name="btn_help"  Margin="0,0,101,24" Style="{StaticResource btn_help}"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_help_Click"/>
        <Slider x:Name="sliderLine" HorizontalAlignment="Left" Margin="164,114,0,0" VerticalAlignment="Top" Width="54" Minimum="1" Maximum="100" Value="90" IsSnapToTickEnabled="True"/>
        <CheckBox x:Name="isCopyNull" Content="仅拷贝目标字段为空的要素" HorizontalAlignment="Left" Margin="252,115,0,0" VerticalAlignment="Top" Width="169"/>
        <Label Content="字段设置：" HorizontalAlignment="Left" Margin="15,155,0,0" VerticalAlignment="Top" Width="71"/>
        <DataGrid   x:Name="dg" ItemsSource="{Binding}" AlternatingRowBackground="LightBlue" AlternationCount="2" EnableRowVirtualization="False" CanUserAddRows="False" AutoGenerateColumns="False"  Margin="15,185,19,71">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Width="35" Header="选择"  Binding="{Binding IsCheck}" IsReadOnly="False"/>
                <DataGridTextColumn Width="120" Header="目标字段"  Binding="{Binding FieldName}" IsReadOnly="True"/>
                <DataGridTextColumn Width="65" Header="字段类型" Binding="{Binding FieldType}" IsReadOnly="True" />
                <DataGridTextColumn Width="65" Header="字段长度" Binding="{Binding FieldLength}" IsReadOnly="True" />
                <DataGridComboBoxColumn Header="来源字段" Width="120">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <!--m_associatedConditionsList就是你里面的项，这部分绑定下面的C#项-->
                            <Setter Property="ItemsSource" Value="{Binding Path=m_associatedConditionsList,RelativeSource={RelativeSource AncestorType={x:Type local:AttributeReaderPlus},Mode=FindAncestor}}" />
                            <!--m_associatedConditionsList就是你里面的项，这部分绑定下面的C#绑定数据-->
                            <Setter Property="SelectedValue" Value="{Binding Path=AssociatedConditions}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Path=m_associatedConditionsList,RelativeSource={RelativeSource AncestorType={x:Type local:AttributeReaderPlus},Mode=FindAncestor}}" />
                            <Setter Property="SelectedValue" Value="{Binding Path=item}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btn_select" Style="{StaticResource btn_selectAll}"  Margin="0,163,41,0" Click="btn_select_Click" Height="22" VerticalAlignment="Top" HorizontalAlignment="Right" Width="22" />
        <Button x:Name="btn_unSelect" Style="{StaticResource btn_unSelectAll}"  Margin="0,163,19,0" Click="btn_unSelect_Click" Height="22" VerticalAlignment="Top" HorizontalAlignment="Right" Width="22" />


    </Grid>
</controls:ProWindow>
