<controls:ProWindow x:Class="CCTool.Scripts.MapMenu.AddMapServer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="添加在线地图" Height="550" Width="500" WindowStartupLocation="CenterScreen" Icon="/CCTool;component/Data/Icons/Atoolbox.png"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <TextBox x:Name="textSource_Copy"  TextWrapping="NoWrap" Text="Null"  FontSize="14" TextAlignment="Center"  IsReadOnly="True" Foreground="#FF989696" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="90" Margin="0,31,14,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="120"/>

        <Image x:Name="IndexIamge" Height="86" Margin="0,33,16,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="117" />

        <Label Content="当前地图：" HorizontalAlignment="Left" Margin="15,6,0,0" VerticalAlignment="Top" Width="266"/>
        <TextBox x:Name="textSource" Margin="15,31,160,0" TextWrapping="Wrap" Text="Null" Height="90" FontSize="24" TextAlignment="Center" VerticalAlignment="Top" IsReadOnly="True" Foreground="#FF989696" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <Label Content="选择在线地图：" HorizontalAlignment="Left" Margin="15,126,0,0" VerticalAlignment="Top" Width="375"/>
        <Button x:Name="btn_go" Style="{StaticResource btn_run}" Margin="0,0,12,22"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_Click" />
        <TreeView Margin="15,151,12,73" x:Name="tv"  ItemsSource="{Binding}" SelectedItemChanged="tv_SelectItemChanged" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding mapItems}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding ImageSource}" Width="{Binding Width}" Height="{Binding Width}"/>
                        <TextBlock Text="{Binding MapName}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

    </Grid>
</controls:ProWindow>
