<controls:ProWindow x:Class="CCTool.Scripts.GDBMenu.ClearGDBAll"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="完全清空GDB" Height="280
                    " Width="350"
        WindowStartupLocation="CenterScreen"
        Icon="/CCTool;component/Data/Icons/Atoolbox.png"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <Button x:Name="btn_go" Style="{StaticResource btn_run}" Margin="0,0,47,15"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_Click" />
        <Button x:Name="btn_cancel" Style="{StaticResource btn_cancel}" Margin="54,0,0,15" VerticalAlignment="Bottom" HorizontalAlignment="Left" Click="btn_cancel_Click"/>
        <DataGrid x:Name="dg" ItemsSource="{Binding}" CanUserAddRows="False" AutoGenerateColumns="False" Margin="17,30,19,92">
            <DataGrid.Columns>
                <DataGridTextColumn Width="120" Header="数据类型"  Binding="{Binding GDBItem}" IsReadOnly="True"/>
                <DataGridTextColumn Width="120" Header="数量" Binding="{Binding Number}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="你确定要删除所有数据？想清楚再说！" HorizontalAlignment="Center" Margin="0,0,0,61" RenderTransformOrigin="0.198,0.07" Width="314" Foreground="Red" FontWeight="Bold" Height="25" VerticalAlignment="Bottom"/>
        <Label Content="该数据库下的所有数据：" HorizontalAlignment="Center" Margin="0,5,0,0" VerticalAlignment="Top" Width="314"/>

    </Grid>
</controls:ProWindow>
