<controls:ProWindow x:Class="CCTool.Scripts.CusTool4.ExportPointAndPolyline"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="[确权登记]生成界址点、界址线" Height="270" Width="598"
        WindowStartupLocation="CenterOwner"
        Icon="/CCTool;component/Data/Icons/Atoolbox.png"
>
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>

        <Label Content="输入面要素图层：" HorizontalAlignment="Left" Margin="10,12,0,0" VerticalAlignment="Top" Width="103"/>
        <ComboBox x:Name="combox_fc" Style="{StaticResource ComboBox_icon}" Margin="113,14,39,0" VerticalAlignment="Top" DropDownOpened="combox_fc_DropDown"/>
        <Label Content="输出边界点要素：" HorizontalAlignment="Left" Margin="10,49,0,0" VerticalAlignment="Top" Width="103"/>
        <TextBox x:Name="textPointPath" Margin="113,51,39,0" TextWrapping="NoWrap" Text="" VerticalAlignment="Top" Height="22"/>
        <Button x:Name="btn_go" Style="{StaticResource btn_run}" Margin="0,0,16,18"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_go_Click" />

        <Button x:Name="btn_help" Style="{StaticResource btn_help}" Margin="0,0,98,18" VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_help_Click"/>

        <Button x:Name="openPointButton" Style="{StaticResource btn_openFolder}" Margin="0,51,15,0" HorizontalAlignment="Right" VerticalAlignment="Top" Click="openPointButton_Click"/>
        <Label Content="输出边界线要素：" HorizontalAlignment="Left" Margin="10,84,0,0" VerticalAlignment="Top" Width="103"/>
        <TextBox x:Name="textPolylinePath" Margin="113,86,39,0" TextWrapping="NoWrap" Text="" VerticalAlignment="Top" Height="22"/>
        <Button x:Name="openPolylineButton" Style="{StaticResource btn_openFolder}" Margin="0,86,15,0" HorizontalAlignment="Right" VerticalAlignment="Top" Click="openPolylineButton_Click"/>


    </Grid>
</controls:ProWindow>
