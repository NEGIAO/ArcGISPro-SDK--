<controls:ProWindow x:Class="CCTool.Scripts.LayerPross.ZoomToFeature"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        Title="遍历要素" Height="180" Width="208"
        WindowStartupLocation="CenterOwner" Icon="/CCTool;component/Data/Icons/Atoolbox.png"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CCTool;component/Data/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid>
        <Label Content="当前OID：" HorizontalAlignment="Left" Margin="10,15,0,0" VerticalAlignment="Top" Width="63"/>
        <TextBox x:Name="txt_id" Margin="78,19,11,0" TextWrapping="NoWrap" VerticalAlignment="Top" IsEnabled="False"/>
        <Button x:Name="btn_next" Style="{StaticResource btn_normal}" Margin="0,0,10,27"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="btn_next_click" Width="85">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="/CCTool;component/Data/Icons/run.png" Height="16"/>
                <TextBlock Text="下一个"  Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
        <Button x:Name="btn_last" Style="{StaticResource btn_normal}" Margin="10,0,0,27"  VerticalAlignment="Bottom" Click="btn_last_click" Width="85" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="/CCTool;component/Data/Icons/run.png" Height="16"/>
                <TextBlock Text="上一个"  Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
        <Label Content="缩放比例：" HorizontalAlignment="Left" Margin="10,46,0,0" VerticalAlignment="Top" Width="63"/>
        <TextBox x:Name="txt_size" Margin="148,50,11,0" Text="{Binding ElementName=sliderLine, Path=Value}" TextWrapping="NoWrap" VerticalAlignment="Top" IsEnabled="False"/>
        <Slider x:Name="sliderLine" HorizontalAlignment="Left" Margin="73,50,0,0" VerticalAlignment="Top" Width="75" Minimum="50" Maximum="100" Value="90" IsSnapToTickEnabled="True"/>


    </Grid>
</controls:ProWindow>
